# Legacy v6 UI contract tests

These tests describe the retired BlinQ 6.x UI and asset contracts. They are kept for historical reference but are intentionally excluded by CI (`pytest --ignore=tests/legacy`).

The active web UI is 7.0.0. Current UI smoke/contract coverage lives in `tests/test_v700_ui_contract.py`.
